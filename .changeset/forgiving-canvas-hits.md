---
"@create-font/editor": minor
---

Make node, handle, and path selection and dragging more forgiving with
deterministic, zoom-stable hit resolution, and raise the canvas zoom limit to
1000%. Add an opt-in Visual Debug command that overlays the effective selection
targets for nodes, handles, and paths.
