---
"@create-font/editor": patch
---

Make canvas Up/Down navigation follow variable-width text lines with native selection semantics, and derive nullable glyph focus from the typing caret instead of defaulting inspector state to O.
