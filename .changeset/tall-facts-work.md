---
"create-design": minor
---

Use `create-design` to scaffold workspaces and add projects, and move existing-project operations to the new `design` CLI with `build`, `check`, `dev`, `serve`, and `export` commands.
