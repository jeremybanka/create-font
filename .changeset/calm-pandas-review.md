---
"@create-font/editor": patch
"create-font": patch
---

Expose a product-neutral source review surface with semantic row, navigation adapter, comparison, and guarded selective-commit hooks while preserving create-font glyph review and Diff View.

Initialize create-font source review in an actionable empty state so the first
HEAD-to-working comparison can be requested.
