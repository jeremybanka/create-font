# Foley projects

Source projects for `create-foley`. Each project keeps mix metadata in
`create-foley.json` and procedural layers in `layers/`, making sound design
changes reviewable without committing generated WAV artifacts.
