# `@create-design/editor`

The create-design browser editor. It owns the design document model, canvas and
selection behavior, PDF interaction, editor UI, and browser mounting API. The
`create-design` application supplies filesystem-backed source sessions and
serves the built browser artifact.
